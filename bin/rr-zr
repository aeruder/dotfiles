#!/bin/bash

# Failed commands aborts script
set -e
# Uninitialized expansion aborts script
set -u
# Any command in a pipe failing aborts script
set -o pipefail

exec run-remote --env=DBIC_TRACE --env=SCREEN_LOG_LEVEL --dir='ziprecruiter' --shell=/bin/zsh --host=sandbox "$@"
