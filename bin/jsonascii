#!/usr/bin/env perl

use feature ':5.14';
use strict;
use warnings 'all';
$^W = 1;

use Tie::IxHash;
use Text::xSV;
use JSON::XS;

my $JSONI = JSON::XS->new->canonical->utf8;
my $JSONO = JSON::XS->new->canonical->ascii;

while (my $line = <STDIN) {
my $
}
