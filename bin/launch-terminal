#!/usr/bin/env zsh

if which termite > /dev/null 2> /dev/null ; then
    if [ $# -gt 0 ]; then
        exec termite --exec="$*"
    else
        exec termite
    fi
else
    if [ $# -gt 0 ]; then
        exec konsole -e "$@"
    else
        exec konsole
    fi
fi
