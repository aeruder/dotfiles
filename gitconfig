[user]
    name = Andrew Ruder
    email = andy@aeruder.net
[alias]
    stat = status
    st = ls-files -d -m -o -v --directory --no-empty-directory --exclude-per-directory=.gitignore
    co = checkout
    cia = commit -a
    cpick = cherry-pick
    fl = log --pretty=oneline --decorate --graph --abbrev-commit
    fla = log --pretty=oneline --decorate --graph --abbrev-commit --all
    l = log --abbrev-commit
[color]
    ui = auto
[merge]
    log = True
    conflictstyle = diff3
[push]
    default = tracking
[log]
    decorate = True
[diff]
    renames = copy
[am]
    keepcr = True
[core]
    preloadindex = True
    excludesfile = ~/.gitignore
[pull]
	rebase = true
[sendemail]
    smtpencryption = tls
    smtpserver = smtp.gmail.com
    smtpuser = andy@aeruder.net
    smtpserverport = 587
[url "https://github.com/"]
	pushInsteadOf = git://github.com/
[url "https://github.com/"]
	pushInsteadOf = http://github.com/
